% The displayskip settings must be specified after \begin{document} to take effect
% I ended up just clumping all length-related settings in one file
\setlength{\parindent}{0pt}
\setlength{\mathindent}{0pt}
\setlength{\abovedisplayskip}{1pt}
\setlength{\belowdisplayskip}{1pt}
\setlength{\abovedisplayshortskip}{0pt}
\setlength{\belowdisplayshortskip}{0pt}

\setlist{itemsep=-4.0pt, topsep=0pt}
\setlist[enumerate,1]{label={(\roman*)}}  % use (i), (ii), etc...
